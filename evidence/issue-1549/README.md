# Issue #1549 evidence status

This directory is reserved for evidence from the current semantic-hook redesign
candidate. The prior images were deliberately removed because they prove an
older presentation and artifact; they are not evidence for the current
candidate.

The checkpoint's immutable Jaunder/workflow pin is
`529fbefadee44e0edc1ed999b289586094a740e0`. Its local generation, drift,
package-validation, and thumbnail commands are recorded in
[`PROVING-GROUND.md`](../../PROVING-GROUND.md).

No final Studio lifecycle, canonical workflow artifact, route capture,
accessibility scan, browser matrix, or visual approval has yet been recorded
for this candidate. A dedicated evidence run must recreate the complete matrix
listed in `PROVING-GROUND.md` from the canonical workflow ZIP and retain its
screenshots and raw command/log references here. It must not reuse the deleted
captures as final proof.
